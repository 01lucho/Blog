<%= simple_form_for([@post, @post.comments.build], remote: true, html:{class: "form-horizontal"}) do |f| %>

<% if user_signed_in? %>
   Nick: <b><%=current_user.nombre_completo %></b>
    <%= f.hidden_field :alias, :value => current_user.nombre_completo %>
<% else %>
  <%= f.input :alias %>
<% end%>
    <%= f.input :comment, :input_html => { :rows => 7}%>
   <%= f.button :submit %>
<% end %>
